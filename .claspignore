# Git worktrees (clasp가 중복 파일 감지하는 것 방지)
.claude/worktrees/**
.worktrees/**

# Agent/tool scratch files (GAS 업로드 금지)
.superpowers/**
.gstack/**
test/**
tools/**

# GitHub Pages (GAS에 올릴 필요 없음)
docs/**

# 레거시 UI — ?page= 라우팅 폐지로 서빙 경로가 없고 village2026 write 호출이
# 하드코딩돼 있어 GAS에 올리지 않는다. 저장소에는 정적 테스트 대상으로 유지 (2026-08-19).
dashboard.html
requestManage.html
timelineMobile.html

# Vercel 앱 (GAS와 무관)
apps/**

# Toss Front plugin (GAS와 무관, config.js/asset이 Apps Script에 올라가면 안 됨)
toss-front-plugin/**

# 로컬 보조/실험 파일. GAS가 최종본이므로 원격에 없는 파일은 자동 업로드 금지.
companionServer.js
companionSidebar.html
companionValidator.js
createManual.js
setupSchedule.js
villageAiClient.js

# 기타
node_modules/**
wiki/**
scripts/**
.git/**
*.md

{
  "name": "village-ai-browser-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI-first Kakao browser worker scaffold for Supabase jobs and Google Sheets API writes.",
  "scripts": {
    "test": "node --test worker.test.mjs",
    "check": "node --check worker.mjs && node --check worker.test.mjs",
    "dry-run": "node worker.mjs --dry-run --once"
  },
  "engines": {
    "node": ">=18"
  }
}

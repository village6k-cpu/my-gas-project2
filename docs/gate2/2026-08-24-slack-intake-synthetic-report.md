# Gate 2 합성 Slack 직원 인입 검증 보고서

## 결론

최종 판정은 **PASS**다. `village-tax-document-clerk`의 합성 Slack 형태 이벤트가 Gate 1의 고정
읽기 전용 `desktop_readiness`를 한 번 실행했고, 로컬 가짜 전달구에 결과를 한 번 전달했으며,
동일 이벤트 재처리는 실행과 전달 없이 `DUPLICATE`로 억제됐다. 실행기 소유 임시 원장도
삭제됐다.

이번 검증은 실제 Slack과 홈택스에 연결하지 않았다. Slack 앱 설치·토큰·서명·메시지 발송과
홈택스 로그인·조회·발급·수정은 모두 범위 밖이었다.

## 구현 범위

- `tools/local-cua-clerk/gate2/slack-intake-shell.mjs`
  - 합성 전용 정확 스키마, 팀·채널 허용 경로, 고정 액션 검증
  - 요청 ID 해시, 이벤트 전체 해시 결합, `0700`/`0600` 로컬 원장
  - 원자적 최초 점유, 완료 중복 억제, 전달만 재개
  - 전달 제한시간과 `delivery_unknown` 자동 재전송 금지
  - 고정 불리언 영수증과 원문 비보존
- `tools/local-cua-clerk/gate2/synthetic-runner.mjs`
  - Gate 1 읽기 전용 실행 1회
  - 로컬 가짜 결과 전달 1회
  - 동일 이벤트 2회차 중복 억제 확인
  - 실행기 소유 임시 원장 상태 확인 및 삭제

## 실실행 이력

1. `2026-08-24T13:33:24.264Z` — 첫 합성 실행은 `BLOCKED/gate2_not_ready`였다.
   Gate 1 준비 상태만 통과하지 못했고, 실행 기록·중복 억제·전달 1회·원장 완료·원장 삭제는
   모두 참이었다. 이 기록은
   `docs/gate2/2026-08-24-slack-intake-synthetic-attempt-1.json`에 보존했다.
2. `2026-08-24T13:33:40.602Z` — 원인 분리용 Gate 1 고정 읽기 전용 확인이 `PASS`였고
   9개 증거가 모두 참이었다. 화면 조작은 없었다.
3. `2026-08-24T13:33:48.219Z` — 최초 Gate 2 전체 합성 재검증이 `PASS`였다.
4. 독립 리뷰에서 변형 스레드 재생과 손상 원장의 교차 조건 문제를 재현했고, 이벤트 전체 해시
   결합과 전달 전 원장 검증으로 수정한 뒤 각각 회귀 테스트를 추가했다.
5. `2026-08-24T13:38:44.910Z` — 리뷰 수정이 반영된 최신 코드의 Gate 2 전체 합성 검증이
   다시 `PASS`였다. 최종 증거는
   `docs/gate2/2026-08-24-slack-intake-synthetic-evidence.json`이다.

첫 시도는 준비 상태의 일시적 실패로 처리됐으며 성공으로 승격하지 않았다. 두 증거 파일은
각각 원래 상태와 시각을 유지하고 strict serializer로 왕복 검증한다.

## 검증 결과

- Gate 0~2 전체 Node 테스트: 92개 통과, 실패 0개
- Gate 2 상태머신·실행기 테스트: 21개 통과, 실패 0개
- `git diff --check`: 통과
- 실제 Slack 연결: `false`
- 홈택스 작업: `false`
- 합성 요청 실행: 1회
- 합성 결과 전달: 1회
- 동일 이벤트 재실행·재전달: 0회
- 실행기 소유 임시 원장 삭제: 확인

## 다음 게이트

Gate 3에서만 실제 Slack 커넥터를 검토한다. 시작 조건은 앱 설치 대상 워크스페이스·채널의
명시적 승인, Slack 서명 또는 Socket Mode 인증, 외부 멱등 키/메시지 조회를 통한 애매한 전달
복구, 최소 권한, 실제 발송 결과 readback이다. 이 조건 전에는 실제 Slack 메시지를 읽거나
쓰지 않는다. 홈택스 업무 자동화는 그 뒤 별도 승인 게이트로 유지한다.
